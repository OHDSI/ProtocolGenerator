# getCdCols

A list of colDefs for the columns used in cohort diagnostic protocol
module

## Usage

``` r
getCdCols()
```

## Value

A list of colDefs for cohort diagnostic

## Details

Returns a list of colDefs

## See also

Other ColDefs: [`cmColDef()`](cmColDef.md),
[`cmOutcomeColDef()`](cmOutcomeColDef.md),
[`defaultColumns()`](defaultColumns.md),
[`getCIcolumns()`](getCIcolumns.md),
[`getPlpColDefs()`](getPlpColDefs.md),
[`getSccsColDefs()`](getSccsColDefs.md)
