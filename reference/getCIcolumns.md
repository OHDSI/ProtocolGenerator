# getCIcolumns

A reactable colDef list for the cohort incidence tables

## Usage

``` r
getCIcolumns()
```

## Value

A list of colDefs for the target and outcome tables describing the
cohort incidence analysis

## Details

Returns a list of colDefs

## See also

Other ColDefs: [`cmColDef()`](cmColDef.md),
[`cmOutcomeColDef()`](cmOutcomeColDef.md),
[`defaultColumns()`](defaultColumns.md), [`getCdCols()`](getCdCols.md),
[`getPlpColDefs()`](getPlpColDefs.md),
[`getSccsColDefs()`](getSccsColDefs.md)
